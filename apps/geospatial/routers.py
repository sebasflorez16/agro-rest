# eos_api/urls.py
"""from django.urls import path, include
from rest_framework import routers
from .geo_api.views.views import EosdaDataView

router = routers.DefaultRouter()
router.register(r'eos-search', EosdaDataView, basename='eos-search')

urlpatterns = [
    path('', include(router.urls)),
]
"""